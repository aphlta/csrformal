# csrformal 本机跑数 2026-09-03

数字全部来自这次实跑，不抄旧报告。`out/` 已 gitignore，完整
Markdown / JSON 在 `out/reports/compliance-2026-09-03.{md,json}`，
变异回归在 `out/reports/self-test-2026-09-03.md`。本摘要进仓库以便跟踪。

## 环境

- RTL 树：`/ssdhome/maoweiming/xiangshan-work/XiangShan-b90dbba` @ `b90dbba4`
  （git HEAD `b90dbba40d16d54f2814870ff8b4d8809a41e680`）
- 规范基线：`riscv/riscv-isa-manual` @ `a5be4cfb5aa9`（2026-09-02T08:15:50Z）
- 生成时间：2026-09-03 11:00:07（`check all` 报告头）
- 仓库提交：见 tag `v0.1`（本摘要与 `expect_kill` 精确匹配同一提交）

## `check all`（302 条）

```
==== 共 302 条：通过 300，反例 2，真空 0，未知 0，错误 0 ====
求解器累计 4.45s，壁钟 9.5s
```

| 模块 | 条数 | 结论 |
|---|---|---|
| CSRPermitModule | 156 | S3 + EQ-permit 仍红（`vstimecmp` + `menvcfg.STCE=0`） |
| TrapHandleModule | 140 | 全绿 |
| TrapEntryMEventModule | 3 | 全绿（EQ-next / EQ-tval / EQ-tval-data） |
| TrapEntryHSEventModule | 3 | 全绿（EQ-next / EQ-tval / EQ-tval-data） |
| **合计** | **302** | 300 HOLDS / 2 VIOLATED / 0 VACUOUS / 0 UNKNOWN / 0 ERROR |

S3+EQ 仍红、TrapEntry 绿：与这次 `check all` 一致。这不是全规范证明。

## 变异体（完整 13/13）

登记 **13** 个（CSRPermit 5 + TrapHandle 5 + TrapEntryM 2 + TrapEntryHS 1）。
本轮跑完整 `./bin/csrformal self-test`（不是 `--only tehs1`）：

```
==== 变异回归：13/13 符合预期 ====
壁钟 114.7s
```

`/usr/bin/time`：`TIME_EXIT=0 WALL_SEC=114.78`。

| 变体 | 类型 | 模块 | 相关性质 | 反例 | 结果 | 说明 |
|---|---|---|---:|---:|---|---|
| `pc1` | fix | CSRPermit | 2 | 0 | FIXED | `S3` + `EQ-permit` 转为通过 |
| `m1` | defect | CSRPermit | 2 | 2 | KILLED | `S1b` + `EQ-permit` |
| `m2` | defect | CSRPermit | 5 | 5 | KILLED | `C2` 族 |
| `m3` | defect | CSRPermit | 5 | 5 | KILLED | `E3` 族 + `EQ-permit` |
| `m4` | defect | CSRPermit | 5 | 5 | KILLED | `C3` 族 |
| `tehs1` | defect | TrapEntryHS | 1 | 1 | KILLED | 只对 `EQ-tval`（htval 误用 trapPcGPA） |
| `te1` | defect | TrapEntryM | 1 | 1 | KILLED | 只对 `EQ-tval`，未吃到 `EQ-tval-data` |
| `te2` | defect | TrapEntryM | 1 | 1 | KILLED | 只对 `EQ-tval-data` |
| `t1` | defect | TrapHandle | 36 | 36 | KILLED | `D2`/`D5` |
| `t2` | defect | TrapHandle | 18 | 18 | KILLED | `D4` |
| `t3` | defect | TrapHandle | 9 | 9 | KILLED | `D6` |
| `t5` | defect | TrapHandle | 1 | 1 | KILLED | `DT2` |
| `pc3` | defect | TrapHandle | 3 | 3 | KILLED | `V3` |

`expect_kill=["EQ-tval"]` 现为后缀精确匹配（或 `key[` 族），不再
`startswith("EQ-tval")`。因此 te1 相关性质 1 条，te2 相关性质 1 条。

## 其它本机检查

| 命令 | 结果 |
|---|---|
| `python3 -m unittest discover -s tests -v` | 28 tests OK（0.061s） |
| `./bin/csrformal lint` | 性质 302 条，规则 80 / 规范 2546，问题 0 处 |
| `./bin/csrformal spec-selfcheck` | 通过 |
